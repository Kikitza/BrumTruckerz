# Kopiraj u `.env` (gitignore) i popuni DEV vrednostima.
# Vrednosti: Supabase Dashboard -> Project Settings -> API (URL + anon/public key),
# ili CLI: `supabase projects api-keys --project-ref <ref>`.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
